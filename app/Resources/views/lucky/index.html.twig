{% extends 'base.html.twig' %}
{% block body %}



                        {% embed 'lucky/table.html.twig' with {'caption' : 'Jogos de hoje', 'table':  table } %}
                        {% endembed %}
                        {% embed 'lucky/table.html.twig' with {'caption' : 'Jogos de amanhã', 'table':  tomorrowGames } %}
                        {% endembed %}

    <script>
            user = {{ user|json_encode()}};
            {% for key,value in data %}

            dataToDraw('{{ value|json_encode() }}', '#placeholder{{ key }}', true);

            {% endfor %}

            {% for key,value in OU %}
            dataToDraw('{{ value|json_encode() }}', '#placeholderGoals{{ key }}', false);
            {% endfor %}
    </script>

{% endblock %}